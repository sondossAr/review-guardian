# ========================================
# Streamlit Cloud Secrets (Example)
# ========================================
# Copiez ce fichier dans l'interface Streamlit Cloud
# Settings → Secrets

[general]
model_path = "./models"
confidence_threshold = 0.7

# Ajoutez d'autres secrets si nécessaire
# [api]
# api_key = "votre-cle-api"
